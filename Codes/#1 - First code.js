//Arrays

var list;

function lst(which) {
    if (which == "names") {
        return ['amanda', 'bruna', 'camila', 'daniela', 'luiza']; 
    }
    
    else if (which == "nums") {
        return [3, 4, 2, 1, 5, 6, 2]
    }

};

var teste = require();

lista = lst('nums');

console.log(lista);